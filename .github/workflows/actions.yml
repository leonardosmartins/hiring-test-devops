name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: node:10.16-jessie
      env:
        ENV: teste
    steps:
    - uses: actions/checkout@v2
    - name: Set directory
      run: cd terraform/modules/lambda/app
    - name: Build
      run: npm install --only=production
    - name: Teste
      run: echo $ENV
